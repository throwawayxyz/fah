name: fah

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: fahdocker
      uses: johnktims/folding-at-home@latest
      with:
        gpu: false
